# Sample LangChain RAG Chain

A minimal LCEL chain that uses a fake in-memory retriever. Demonstrates the
LangChain adapter's callback-based trace capture.

```bash
pip install agentguard[langchain]
agentguard run scenarios/rag_policy_agent
```
